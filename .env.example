# ╔══════════════════════════════════════════════════════════════╗
# ║  Personal Meeting Assistant — Environment Configuration     ║
# ╚══════════════════════════════════════════════════════════════╝
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# See README.md for a full setup walkthrough.

# ── OpenAI (required) ────────────────────────────────────────
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-5.1-2025-11-13
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_MAX_CONCURRENT=3
OPENAI_DAILY_TOKEN_BUDGET=500000

# ── PostgreSQL ─────────────────────────────────────────────────
POSTGRES_PASSWORD=postgres
# For local dev (backend runs on host), use localhost:
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/meeting_assistant
# For Full Docker mode (all services in containers), use the service name:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/meeting_assistant

# ── Neo4j ──────────────────────────────────────────────────────
NEO4J_PASSWORD=password
NEO4J_USER=neo4j
# For local dev:
NEO4J_URI=bolt://localhost:7687
# For Full Docker mode:
# NEO4J_URI=bolt://neo4j:7687

# ── Redis ──────────────────────────────────────────────────────
# For local dev:
REDIS_URL=redis://localhost:6379/0
# For Full Docker mode:
# REDIS_URL=redis://redis:6379/0

# ── Granola ─────────────────────────────────────────────────────
# Path to Granola's local cache file (used as fallback when MCP is unavailable).
# macOS (default):
GRANOLA_CACHE_PATH=~/Library/Application Support/Granola/cache-v3.json
# Linux:
# GRANOLA_CACHE_PATH=~/.config/Granola/cache-v3.json
# Windows (use forward slashes):
# GRANOLA_CACHE_PATH=C:/Users/YourName/AppData/Roaming/Granola/cache-v3.json

# Official Granola MCP endpoint (requires Business plan, auth via UI)
GRANOLA_MCP_URL=https://mcp.granola.ai/mcp

# ── Google Calendar (optional) ────────────────────────────────
# Get these from Google Cloud Console — see README.md for step-by-step instructions.
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret

# ── Security ──────────────────────────────────────────────────
# Encryption key for storing sensitive data (API keys, OAuth tokens) at rest.
# Generate one with:
#   python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=generate-a-key-see-command-above

# ── App Settings ───────────────────────────────────────────────
SYNC_BATCH_SIZE=10
SYNC_INTERVAL_MINUTES=15

# ── Server ─────────────────────────────────────────────────────
# UVICORN_WORKERS=1
